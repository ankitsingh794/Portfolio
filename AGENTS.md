# Portfolio Project

## Path
```
C:/Project/webdev/idk/Some crazy things/Portfolio/Portfolio
```

## Commands
- Run dev server: `npm run dev` (in Portfolio subdirectory)
- Install dependencies: `npm install <package>`

## Path Quirk
Working directory has spaces. Always use `workdir` parameter in bash commands instead of `cd`:
```bash
{ "command": "npm run dev", "workdir": "C:/Project/webdev/idk/Some crazy things/Portfolio/Portfolio" }
```

## Stack
- React 19 + Vite
- GSAP for animations
- Components in `src/components/`

## Current Work
Building a futuristic single-page scroll portfolio for Ankit Singh (Full-Stack Developer). Uses Aurora background, holographic glassmorphism.